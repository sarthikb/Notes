Absolutely! Let’s go step-by-step to understand **Kruskal’s Algorithm**, then I’ll provide Java code with an example and explain it using a **graphical representation**.

---

## 🧠 What is Kruskal's Algorithm?

**Kruskal's Algorithm** is a **greedy algorithm** used to find the **Minimum Spanning Tree (MST)** of a connected, undirected, and weighted graph.

### ✅ Goal:

To connect all vertices with the **minimum possible total edge weight**, **without forming cycles**.

---

## 📌 Steps of Kruskal's Algorithm

1. **Sort all edges** of the graph in **non-decreasing order** of their weights.
2. Initialize a **disjoint set (Union-Find)** for each vertex.
3. Traverse through the sorted edges and:

   * **If the edge connects two different components** (i.e., it doesn’t form a cycle), **add it to the MST**.
   * **Otherwise, skip it**.
4. Stop when the MST contains exactly `V - 1` edges, where `V` is the number of vertices.

---

## 📈 Graphical Example

Let’s take a sample graph:

```
  (A)---4---(B)
   | \       |
   1  3      2
   |    \    |
  (C)---5---(D)
```

### Edges with weights:

* A - C: 1
* A - D: 3
* A - B: 4
* C - D: 5
* B - D: 2

### Step-by-step (Sorted edges):

1. A - C: 1
2. B - D: 2
3. A - D: 3
4. A - B: 4
5. C - D: 5

### Building MST:

* Add A - C (1) ✅
* Add B - D (2) ✅
* Add A - D (3) ✅
* Stop (3 edges added for 4 nodes = MST complete)

### Final MST:

```
  (A)     (B)
   |       |
   1       2
   |       |
  (C)---3--(D)
```

---

## 💻 Java Code Implementation

Here is the full Java implementation of Kruskal's Algorithm with the above example.

```java
import java.util.*;

class Edge implements Comparable<Edge> {
    int src, dest, weight;

    public Edge(int s, int d, int w) {
        src = s;
        dest = d;
        weight = w;
    }

    public int compareTo(Edge compareEdge) {
        return this.weight - compareEdge.weight;
    }
}

class Subset {
    int parent, rank;
}

public class KruskalMST {

    int vertices;
    ArrayList<Edge> edges = new ArrayList<>();

    KruskalMST(int v) {
        vertices = v;
    }

    void addEdge(int s, int d, int w) {
        edges.add(new Edge(s, d, w));
    }

    int find(Subset[] subsets, int i) {
        if (subsets[i].parent != i)
            subsets[i].parent = find(subsets, subsets[i].parent);
        return subsets[i].parent;
    }

    void union(Subset[] subsets, int x, int y) {
        int xroot = find(subsets, x);
        int yroot = find(subsets, y);

        if (subsets[xroot].rank < subsets[yroot].rank)
            subsets[xroot].parent = yroot;
        else if (subsets[xroot].rank > subsets[yroot].rank)
            subsets[yroot].parent = xroot;
        else {
            subsets[yroot].parent = xroot;
            subsets[xroot].rank++;
        }
    }

    void kruskalMST() {
        ArrayList<Edge> result = new ArrayList<>();
        Collections.sort(edges);

        Subset[] subsets = new Subset[vertices];
        for (int i = 0; i < vertices; ++i) {
            subsets[i] = new Subset();
            subsets[i].parent = i;
            subsets[i].rank = 0;
        }

        int e = 0, i = 0;
        while (e < vertices - 1 && i < edges.size()) {
            Edge next = edges.get(i++);

            int x = find(subsets, next.src);
            int y = find(subsets, next.dest);

            if (x != y) {
                result.add(next);
                union(subsets, x, y);
                e++;
            }
        }

        System.out.println("Edges in MST:");
        int totalWeight = 0;
        for (Edge edge : result) {
            System.out.println((char)(edge.src + 'A') + " - " + (char)(edge.dest + 'A') + " : " + edge.weight);
            totalWeight += edge.weight;
        }
        System.out.println("Total weight: " + totalWeight);
    }

    public static void main(String[] args) {
        KruskalMST graph = new KruskalMST(4);  // Vertices: A=0, B=1, C=2, D=3

        graph.addEdge(0, 2, 1);  // A - C
        graph.addEdge(1, 3, 2);  // B - D
        graph.addEdge(0, 3, 3);  // A - D
        graph.addEdge(0, 1, 4);  // A - B
        graph.addEdge(2, 3, 5);  // C - D

        graph.kruskalMST();
    }
}
```

---

## 📝 Output

```
Edges in MST:
A - C : 1
B - D : 2
A - D : 3
Total weight: 6
```

---

## 🧩 Vertex Mapping

* `0 = A`
* `1 = B`
* `2 = C`
* `3 = D`

---

## ✅ Summary

* Kruskal’s is **edge-based**, ideal for **sparse graphs**.
* Requires **Union-Find** for cycle detection.
* Total MST weight here: **6**
